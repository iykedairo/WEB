pt.ready = function(p, s, body, head, T, w){var fls;
var isHTML = pt.isHTML;
var isCollection = pt.isCollection;
var isList = pt.isList; var isNode = pt.isNode;
setTimeout(function(){ 
pt(".fly-in-text").Attr("class", "hidden");


pt("body").Attr("Techno", "Michael Techie PT PrizeT");
	pt(".fly-in-text").Attr("class", "flight fly   flys flight flew flies");
	

}, 500);

function prop(elem, name, value) { var ret, hooks, nType = elem.nodeType;
 // Don't get/set properties on text, comment and attribute nodes 
 if ( nType === 3 || nType === 8 || nType === 2 ) { return; } 
    if ( value !== undefined ) { 
if ( hooks && "set" in hooks && ( ret = hooks.set( elem, value, name ) ) !== undefined ) { 
    	return ret; } 
    	return ( elem[ name ] = value ); }
if ( hooks && "get" in hooks && ( ret = hooks.get( elem, name ) ) !== null ) { return ret; } 
    	 return elem[ name ]; 
}


};
