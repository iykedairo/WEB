var Custom = (function() {
// private data variables
var t, PT, Pt, pT;
var pointers = [pt, PT, Pt, pT];


return {
// public methods and properties
};
 }());


function Person(name) {

	this.name = {PT, Pt, pT, pt};
  
if (this instanceof Person) {
// called with "new"
} else {
// called without "new"
}
}