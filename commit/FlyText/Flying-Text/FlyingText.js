pt.ready = function(p, s, body, head, T, w){
var isHTML = pt.isHTML;
var isCollection = pt.isCollection;
var isList = pt.isList; var isNode = pt.isNode;
pt(".fly-in-text").Attr("class", "hidden");
pt("body").Attr("Techno", "Michael Techie PT PrizeT");
    pt(".fly-in-text").Attr("class" ,"flight fly flys flight flew flies").Attr("id", "gig");
pt("gig").each(function(){
    pt.stringify(pt.computedStyle(this))
});

setTimeout(function(){ 
window.Techno = pt.Techno;
var h = pt("#err h4").Attr("data-datum", "set");
function getElementsByAttribute(attribute, context){
    var elements = document.querySelectorAll(context || "*" + "[" +attribute + "]");
    pt.mixin(elements, pt.element);
    return Array.prototype.slice.call(elements);
}
var all = getElementsByAttribute("data-datum"); 
// pt.css(this, "color", "crimson");
}.bind(this), 2000);
};



