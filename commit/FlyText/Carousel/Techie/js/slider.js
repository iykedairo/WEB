Techie(foo);
// Plug N Play

function foo ( body, head, doc, $, _, win )  {
    // body...
    var tem = $("#img");
    var tem2 = $("#empty");
function slide ( e , ind, dirr) {
    e = e.isList() ? e[0] : e;
    if (typeof hasRun === "undefined") { var hasRun = true; e.css({width: "15em", position: "relative"});}
  e.style.left   = ind + "px";

}

slide (tem2,  200)


}